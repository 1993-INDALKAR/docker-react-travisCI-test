sudo: required
language: generic
services:
    - docker build -t harishindalkar/docker-react-travisci-test -f Dockerfile.dev .
    
script:
    # - docker run -e CI=true harishindalkar/docker-react-travisCI-test npm run test

    - docker run harishindalkar/docker-react-travisci-test npm run test -- --coverage
